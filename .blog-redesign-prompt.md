你是一个高级前端设计工程师，正在重新设计 nextjs-starter-zoe-app 的 Blog 页面系列。

## 项目背景
- Next.js 16 + Tailwind CSS 4 + shadcn/ui
- 通用产品网站 starter，当前示例是 FlowAI（AI 工作流产品）
- 已有三套主题（vercel/linear/stripe），通过 CSS 主题类选择器适配
- i18n 用 src/lib/i18n.ts 的 getLabel()
- 当前 blog 页面设计偏朴素，需要产品级重设计

## 需要重设计的页面

### 1. Blog 列表页 (src/app/blog/page.tsx)

当前问题：标题+按钮的布局太基础，没有视觉层次。

重设计：
- Hero 区域：大标题 + 描述 + 搜索框（UI 展示用，可以不实际搜索）
- Archives 和 Tags 按钮移到更优雅的位置（可以放在搜索框旁边或页面侧栏）
- 文章列表默认用 grid 模式（2列），带封面图占位
- 支持 grid/list 视图切换按钮
- 特色文章（pinned）置顶，用更大的卡片突出展示（横跨两列）
- 添加主题语义类名 blog-page

### 2. 文章详情页 (src/app/blog/[slug]/page.tsx)

当前问题：
- 日期格式硬编码中文 zhCN
- 返回按钮太普通
- 文章头部缺乏视觉冲击
- Author Card 在文末太突兀

重设计：
- 如果文章有 banner，做成全宽的封面图 hero（带渐变叠加层 + 标题在图上）
- 如果没有 banner，用渐变色背景 hero
- 文章元信息（日期、阅读时间、标签）设计更精致
- 日期格式国际化（用 getLabel 读取格式或用相对时间）
- 返回按钮改为 breadcrumb 风格（Blog > 文章标题）
- 底部作者区域改为更产品化的设计（头像+名字+简介一行，不要占太多空间）
- 上一篇/下一篇导航
- prose 样式微调确保跟主题协调
- 添加主题语义类名 blog-post

### 3. 归档页 (src/app/blog/archives/page.tsx)

当前问题：时间线设计太基础。

重设计：
- 左侧年份作为大号数字固定显示
- 右侧文章列表用卡片风格
- 整体用时间线设计（vertical timeline 带连接线和节点）
- 添加主题语义类名

### 4. 标签页 (src/app/blog/tags/page.tsx)

当前问题：Badge 列表太简单。

重设计：
- 标签云风格：标签大小根据文章数量变化
- 或者用卡片网格：每个标签一个卡片，显示文章数量和最新文章标题
- 添加主题语义类名

### 5. 标签详情页 (src/app/blog/tag/[slug]/page.tsx)

当前问题：硬编码中文，设计太基础。

重设计：
- 标签名作为 Hero 标题
- 文章数量统计
- 文章列表用 grid 模式
- 所有文字国际化
- 添加主题语义类名

### 6. themes.css 主题适配

在 src/styles/themes.css 中添加 blog 相关的主题适配：
- .theme-vercel .blog-page — 极简风格
- .theme-linear .blog-page — 暗色光晕
- .theme-stripe .blog-page — 白底渐变
- 同样适配 blog-post, blog-archives, blog-tags 等类名

## 通用要求
- 所有硬编码中文改为 getLabel() 调用，在 i18n.ts 中添加缺失的 label
- 日期格式不要硬编码 zhCN，用 getLabel 获取日期格式字符串
- 响应式设计（mobile first）
- dark mode 支持
- TypeScript 类型完整
- 设计要大气、现代、产品级，参考 Vercel Blog / Linear Blog / Stripe Blog 的设计感
- 保持向后兼容

完成后 commit，message: feat: redesign blog pages with product-grade design

然后运行通知命令:
/Users/zoe/.nvm/versions/node/v22.22.0/bin/openclaw system event --text "Done: Blog pages redesigned" --mode now
