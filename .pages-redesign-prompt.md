你是一个高级前端设计工程师，正在为 nextjs-starter-zoe-app 全面提升所有页面的设计质量。这是一个通用产品网站 starter，需要产品级设计。

## 项目背景
- Next.js 16 + Tailwind CSS 4 + shadcn/ui
- 三套主题（vercel/linear/stripe），通过 .theme-xxx CSS 类适配
- i18n 用 src/lib/i18n.ts 的 getLabel()
- 当前示例是 FlowAI（AI 工作流自动化产品，theme: vercel）

## 需要重设计的页面和组件

### 1. Header (src/components/layout/header.tsx)

当前问题：功能性够用但视觉普通。

重设计：
- Logo 区域更精致（logo + title 对齐优化）
- 导航链接间距更大，hover 效果更现代（下划线动画或背景色过渡）
- 移动端底部导航栏设计更精致（图标 + 文字的间距，active 状态用圆点或下划线而不只是颜色变化）
- "更多" 菜单文字国际化
- 添加 site-header 主题类名（已有）确保主题适配
- 可选：导航栏滚动时添加更明显的背景模糊和阴影

### 2. Footer (src/components/layout/footer.tsx)

当前问题：基本结构 OK 但不够大气。

重设计：
- 上半部分：Logo + 简短描述 + 社交图标（左侧），链接分组（右侧）
- 下半部分：版权信息居中
- 去掉 "Made with heart by" 个人站风格的 author popover（产品站不需要）
- 社交图标 hover 效果更精致
- 链接分组排版更宽敞
- 添加分隔线装饰
- 硬编码中文（如 "扫码添加微信"）国际化
- 整体间距加大，呼吸感更强

### 3. Projects 列表页 (src/app/projects/page.tsx)

当前问题：标题 + 列表太简单。

重设计：
- Hero 区域：大标题 + 描述 + 项目数量统计
- 语言筛选器设计更现代（pill 按钮组）
- 整体加上 page-projects 主题类名

### 4. Pricing 页面 (src/app/pricing/page.tsx + src/components/pricing.tsx)

需要看一下 pricing 组件的实际代码再决定改多少，但至少：
- 页面级的 Hero 区域（大标题 + 描述）
- 如果组件内部设计不够好，也一起改
- 添加 page-pricing 主题类名

### 5. Changelog 页面 (src/app/changelog/page.tsx + src/components/changelog.tsx)

重设计：
- 时间线设计：左侧版本号 + 日期，右侧内容卡片
- 版本号作为视觉锚点（大号字体、badge 样式）
- 最新版本突出显示
- 空状态设计更优雅
- 添加主题类名

### 6. Releases 页面 (src/app/releases/page.tsx + src/components/release-card.tsx)

重设计：
- 类似 GitHub Releases 的现代设计
- 每个 release 是一个卡片：版本号 + 日期 + release notes + 下载资产列表
- 最新版本置顶突出
- 添加主题类名

### 7. Help 帮助中心 (src/app/help/page.tsx, src/app/help/categories/[id]/page.tsx, src/app/help/item/[id]/page.tsx, src/components/help.tsx)

重设计：
- 搜索框置顶（UI 展示用）
- 分类用图标卡片网格
- 文章列表更整洁
- 添加主题类名

### 8. Dynamic pages (src/app/[...slug]/page.tsx)

重设计：
- 内容页增加 TOC (table of contents) 侧栏（如果标题多的话）
- 标题区域更有视觉层次
- prose 样式优化
- 添加主题类名

### 9. 404 / Error 页面 (src/app/not-found.tsx, src/app/error.tsx)

当前的 404 还行但可以更精致：
- 保持动画效果但让设计更产品化
- Error 页面国际化
- 添加主题适配

### 10. Drafts 页面 (src/app/blog/drafts/page.tsx)

- 快速优化设计，跟 blog 列表页风格统一

### 11. themes.css 补充

在 src/styles/themes.css 中为以上所有新增的主题类名添加主题适配样式。

## 通用设计原则
- 每个独立页面都要有一个 Hero/Header 区域（大标题 + 描述），不要直接裸标题
- 所有页面的间距和排版要统一
- 卡片设计要统一风格（圆角、阴影、hover 效果）
- 空状态设计要优雅（图标 + 文字 + 引导操作）
- 所有硬编码中文改为 getLabel() 调用
- 响应式设计（mobile first）
- dark mode 支持
- TypeScript 类型完整

## 注意事项
- 保持向后兼容
- 不要修改 blog 相关页面（已在另一个任务中处理）
- 不要修改首页 page.tsx 和 section 组件（已经改好了）
- 如果某个组件文件太大不好改，可以只改关键视觉部分
- commit message: feat: redesign all pages with product-grade design

完成后运行通知命令:
/Users/zoe/.nvm/versions/node/v22.22.0/bin/openclaw system event --text "Done: All pages redesigned" --mode now
